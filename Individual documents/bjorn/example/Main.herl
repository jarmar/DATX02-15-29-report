module Main where
imported
    Client
        startServer as start :: IO () -- Optional type signatures
        runClient   as cli   :: String -> IO ()
exported
    main :: IO ()

main :: IO ()
main = do
    cli "nope"
    ser
    cli "hi"
